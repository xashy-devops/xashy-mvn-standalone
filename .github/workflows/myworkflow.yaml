name: Maveninstall
on:[workflow_dispatch]

jobs:
  installjava:
    runs-on: self-hosted
    steps: 
      - name: install java 
        run: sudo yum install java -y 
      - name: install maven
        run: sudo yum install maven -y 
    
          
